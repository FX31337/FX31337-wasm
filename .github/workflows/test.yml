---
name: Test

# yamllint disable-line rule:truthy
on:
  pull_request:
  push:

jobs:
  Compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: emscripten-core/setup-emsdk@v16
        with:
          version: 'latest'
      - name: Verify emcc
        run: emcc -v
      - name: Build
        run: npm run build
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-files
          path: dist
  Test:
    needs: Compile
    runs-on: ubuntu-latest
    strategy:
      matrix:
        testjs:
          - IndicatorTest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 19.x
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-files
          path: dist
      - run: ls -al
      - run: ls -al dist
      - run: npm install
      - run: npx tsx tests/js/${{ matrix.testjs }}.ts
